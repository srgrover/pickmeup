{% extends 'layout.html.twig' %}

{% block javascripts %}
    {{ parent() }}  {# parent() carga el contenido del bloque y añade lo siguiente #}
    <script src="{{ asset('assets/js/custom/nick-test.js') }}"></script>
{% endblock %}

{% block content %}
    <div class="col-lg-6 box-form">
        <h2>Registrarse</h2>
        <hr>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-group">
            <div class="col-sm-2"></div>
            <div class="col-sm-10">
                <button type="submit" name="enviar" class="btn btn-success">
                    <i class="fa fa-check"></i>
                    Registrarse
                </button>
            </div>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}

